version: "3"
services:
  zookeeper:
    image: bitnami/zookeeper:latest
    ports:
      - "2181:2181"
    environment:
      - ALLOW_ANONYMOUS_LOGIN=yes

  kafka:
    image: bitnami/kafka:latest
    ports:
      - "9092:9092"
    environment:
      - KAFKA_BROKER_ID=1
      - KAFKA_LISTENERS=PLAINTEXT://:9092
      - KAFKA_ADVERTISED_LISTENERS=PLAINTEXT://kafka:9092
      - KAFKA_ZOOKEEPER_CONNECT=zookeeper:2181
      - ALLOW_PLAINTEXT_LISTENER=yes
    depends_on:
      - zookeeper

  postgres-db:
    image: postgres:latest
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_DB=ors
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=pass
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./db:/docker-entrypoint-initdb.d

  admin:
    image: adminer
    restart: always
    depends_on:
      - postgres-db
    ports:
      - 8070:8080

  assignment-service:
    build:
      context: .
      dockerfile: assignment-logic-service/src/main/docker/Dockerfile.jvm
    ports:
      - "8084:8084"
    environment:
      - JAVA_OPTS_APPEND=-Dquarkus.http.host=0.0.0.0 -Djava.util.logging.manager=org.jboss.logmanager.LogManager
    # Uncomment the following lines if you need live code updates without rebuilding the container
    # volumes:
    #   - ./src:/deployments/src
    depends_on:
      - kafka

  order-service:
    build:
      context: .
      dockerfile: order-management-service/src/main/docker/Dockerfile.jvm
    ports:
      - "8082:8082"
    environment:
      - JAVA_OPTS_APPEND=-Dquarkus.http.host=0.0.0.0 -Djava.util.logging.manager=org.jboss.logmanager.LogManager
    # Uncomment the following lines if you need live code updates without rebuilding the container
    # volumes:
    #   - ./src:/deployments/src
    depends_on:
      - kafka

  printer-service:
    build:
      context: .
      dockerfile: printer-management-service/src/main/docker/Dockerfile.jvm
    ports:
      - "8083:8083"
    environment:
      - JAVA_OPTS_APPEND=-Dquarkus.http.host=0.0.0.0 -Djava.util.logging.manager=org.jboss.logmanager.LogManager
    # Uncomment the following lines if you need live code updates without rebuilding the container
    # volumes:
    #   - ./src:/deployments/src
    depends_on:
      - kafka

volumes:
  postgres-data:
